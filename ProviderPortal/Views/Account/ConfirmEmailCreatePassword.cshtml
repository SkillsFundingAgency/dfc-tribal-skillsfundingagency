@{
    ViewBag.Title = AppGlobal.Language.GetText(this, "Title", "Confirm Email");
}

<h2>@AppGlobal.Language.GetText(this, "Header", "Confirm Email")</h2>
<div>
    <p>
        @AppGlobal.Language.GetText(this, "MessageOpening", "Thank you for confirming your email. Please ")
        @Html.ActionLink(AppGlobal.Language.GetText(this, "MessageLinkText", "click here to Log in"), "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })
        @AppGlobal.Language.GetText(this, "MessageClosing", ".")
    </p>
</div>
