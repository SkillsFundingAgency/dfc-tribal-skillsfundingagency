@model Tribal.SkillsFundingAgency.ProviderPortal.Models.AddEditDeliveryLocationViewModel
@{
    ViewBag.Title = AppGlobal.Language.GetText(this, "Title", "Add New Delivery Location");
}

<h2>@AppGlobal.Language.GetText(this, "Header", "Add New Delivery Location")</h2>

@Html.Partial("_EditDeliveryLocation", Model)

@section Scripts {
   
    @Html.Partial("_EditDeliveryLocationScripts", Model)
}